#include <cstdio>

#define K 100
#define N K
# define _N1 100
#define N3 "fjfjjf"
#define N_ 100
    #define PN printf("\n")
#define SUM for(i=0; i<N; i++) sum += i
#define FUNC(   x ,  y   ) ((x)+(y) - \
    N_\
    )

int main () {
    int i, sum = 0;

    #define N1 100
    SUM;
    printf("%dN", sum);
    PN;
    N1;
    int u = FUNC(1,2);
    printf("%d", u);
}